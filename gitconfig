[user]
    name = ankrt
    email = ankrt@mail.com
[push]
    default = simple
[commit]
    verbose = true
[merge]
    ff = only
[core]
    editor = vim
[filter "media"]
    required = true
    clean = git media clean %f
    smudge = git media smudge %f
[alias]
    graph = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

